pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        maven { url 'https://jitpack.io' }
    }
}

include ':app',':pdfgenerator',':easywaylocation',':androidparallax',':crystalrangeseekbar',':spacelib',':swipe-reveal-layout',':boommenu',':asyncexpandablelist',':qrmodule',':passcodeview',':multisearchviewlib',':slycalendarview', ':multiple-images-selector',':sweetdialoglib'

dependencyResolutionManagement {
    //repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven { url "https://jitpack.io" }
        mavenCentral()
        gradlePluginPortal()
    }
}