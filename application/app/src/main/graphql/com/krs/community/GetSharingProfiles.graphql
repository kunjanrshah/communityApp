query GetSharingProfiles($userId: Int!) {
    getSharingProfiles(userId: $userId) {
        id
        first_name
        last_name_id
        member_code
        mobile
        email
        status
    }
}
